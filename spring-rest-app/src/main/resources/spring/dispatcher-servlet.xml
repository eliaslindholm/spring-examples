<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<mvc:annotation-driven>
		<mvc:message-converters register-defaults="true">
			<!-- MappingJacksonHttpMessageConverter is registered automagically if jackson is present on
				 the classpath. But since we need a custom ObjectMapper, we have to explicitly register
				 MappingJacksonHttpMessageConverter configured with our custom ObjectMapper -->
			<bean class="org.springframework.http.converter.json.MappingJacksonHttpMessageConverter">
				<property name="objectMapper" ref="objectMapper" />
			</bean>
		</mvc:message-converters>
	</mvc:annotation-driven>

	<bean id="objectMapper" class="elilin.spring.restapp.CustomObjectMapper" />
	<bean id="messageService" class="elilin.spring.restapp.MessageService" />
</beans>
